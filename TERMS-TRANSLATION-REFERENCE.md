# 术语翻译对照表（LR-0020 审计权威依据）

> 这是给"补齐所有出现的英文术语翻译"任务用的权威对照表。所有翻译严格对齐 `GLOSSARY.md`。
> 审计规则（LR-0020）：英文专业术语缩写**每次出现**都内联 `English (中文)`。
> 例外：① 纯物理单位（μs/MHz/dBm/dB/mW/ms/km 等）不翻译；② 同一段落内连续重复同一术语，第二次起可省；③ 同段已展开过的子组件缩写（KCK/TK 等）不重复。

## 高频术语（必须每次出现都加翻译）

### 协议/层
- `PHY (物理层)`
- `MAC (介质访问控制层)`
- `MLME (MAC 子层管理实体)`
- `OFDM (正交频分复用)`
- `CSMA/CA (载波侦听多址接入 / 碰撞避免)`
- `DCF (分布式协调函数)`
- `EDCA (增强分布式信道接入)`
- `HCF (混合协调函数)`
- `FHSS (跳频扩频)`
- `TDMA (时分多址)`

### 网络结构
- `AP (接入点)`
- `STA (站点)`
- `BSS (基本服务集)`
- `BSSID (基本服务集标识)`
- `SSID (服务集标识)`
- `ESS (扩展服务集)`
- `IBSS (独立基本服务集)`

### 介质访问
- `ACK (确认帧)`
- `RTS/CTS (请求发送 / 允许发送)`
- `NAV (网络分配矢量)`
- `CCA (信道空闲评估)`
- `SIFS (短帧间间隔)`
- `DIFS (DCF 帧间间隔)`
- `AIFS (任意帧间间隔)`
- `AIFSN` — AIFS 的数字参数，写 `AIFSN (AIFS 数)`
- `CW (竞争窗口)`
- `BEB (二进制指数退避)`
- `Backoff (退避)`

### 帧结构
- `FCS (帧校验序列)`
- `PS-Poll (省电轮询帧)`
- `QoS (服务质量)`
- `TID (业务标识符)`
- `AC (接入类别)` — AC_VO/AC_VI/AC_BE/AC_BK 后面首次出现加 `(语音)/(视频)/(尽力而为)/(后台)`

### 省电
- `TIM (业务指示图)`
- `DTIM (投递业务指示图)`
- `U-APSD (非调度自动省电投递)`
- `TWT (目标唤醒时间)`
- `AID (关联标识符)`

### 关联流程
- `Beacon (信标帧)`
- `Probe Request / Probe Response (探测请求 / 探测响应)`
- `Authentication (802.11 认证帧)`
- `Association (关联)`
- `Reassociation (重关联)`
- `Deauthentication / Disassociate (取消认证 / 取消关联)`

### 安全
- `WPA / WPA2 / WPA3 (Wi-Fi 保护访问)` — 首次出现加，后续 WPA2/WPA3 可省括号
- `PSK (预共享密钥)`
- `PMK (成对主密钥)`
- `PTK (成对临时密钥)`
- `GTK (组临时密钥)`
- `CCMP` — 首次加 `(基于 AES 的加密+完整性协议)`
- `TKIP` — 首次加 `(临时密钥完整性协议)`
- `WEP` — 首次加 `(有线等效加密)`
- `MIC (消息完整性码)`
- `EAPOL (局域网上的 EAP)`
- `SAE (对等同步认证)`
- `PMF (受保护管理帧)`
- `RSN-IE (强健安全网络信息元素)`
- `RSNA (强健安全网络关联)`
- `PBKDF2` — 首次加 `(基于密码的密钥派生函数 2)`
- `PWE (密码元素)`
- `KCK` — 首次加 `(密钥确认密钥)`
- `KEK` — 首次加 `(密钥加密密钥)`
- `TK` — 首次加 `(临时密钥)`

### RF/调制
- `RF (射频)`
- `SNR (信噪比)`
- `RSSI (接收信号强度指示)`
- `MCS (调制与编码方案)`
- `QAM (正交幅度调制)` — 16-QAM/64-QAM 等具体阶数前加 `(正交幅度调制)`
- `BPSK (二进制相移键控)`
- `I/Q (同相 / 正交)`
- `PAPR (峰均功率比)`
- `ICI (子载波间干扰)`
- `ISI (符号间干扰)`
- `EVM (误差向量幅度)`
- `EIRP (等效全向辐射功率)`

### 驱动栈
- `SoftMAC (软件 MAC)`
- `FullMAC (全 MAC)`
- `WMM (Wi-Fi 多媒体)`

## 不翻译的（纯单位或通用缩写）
- μs / ms / MHz / GHz / dBm / dB / mW / W / km / m / B (字节)
- EAP（缩写本身是缩写，写 `EAP (可扩展认证协议)` 首次）
- ASCII / CPU / GPU / DMA / RC4 / AES / SHA1 / HMAC / DHCP / ARP / IP / TCP

## 处理原则
1. **遇到英文专业缩写 → 查上表 → 加 `English (中文)` 括号**
2. **同一段内第二次及以后 → 可省略括号**（避免一句话 5 次重复）
3. **已经是中英混合的术语**（如 `Beacon (信标帧)` 已写过）→ 保持不变
4. **不要改技术内容、不要改数字、不要改代码块内的术语**
5. **代码块 `<code>` `<pre>` 内的术语不翻译**（那是代码/命令）
